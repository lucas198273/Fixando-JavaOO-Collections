package exercicios.lista;

public class Program2 {


   /*Em Java, a interface Listfaz parte do framework de coleções do Java, que é uma estrutura de dados usada para armazenar e manipular coleções de objetos. A interface Listé uma coleção ordenada de elementos, onde cada elemento é acessado por seu índice na lista.

    Aqui estão algumas informações importantes sobre a interface Listem Java:

    Ordenação: A Listmantém a ordem de inserção dos elementos. Isso significa que a ordem em que os elementos são adicionados à lista é preservada e pode ser acessada usando índices.

    Duplicatas: A Listpermite a inclusão de elementos duplicados. Ou seja, você pode ter itens repetidos na lista.

    Acesso por índices: A Listpermite o acesso aos elementos pelo índice. Você pode obter o elemento em uma determinada posição da lista usando o método get(index), onde indexé o índice do elemento desejado.

*/ 

/*Em Java, existem várias classes que implementam uma interface List. Algumas das classes mais comuns que implementam uma interface Listsão:

ArrayList: É uma classe que implementa a interface Liste usa uma matriz dinâmica para armazenar elementos em uma lista. É uma das implementações mais populares de Listem Java, pois oferece alta performance em acesso aleatório (por índices) e é eficiente em termos de espaço.

LinkedList: É outra classe que implementa a interface Liste usa uma lista duplamente encadeada para armazenar elementos em uma lista. É mais eficiente em termos de inserção e remoção de elementos no início ou no final da lista, mas pode ter um desempenho mais baixo em acesso aleatório (por índices).

Vector: É uma classe legada que também implementa uma interface Liste */



    
}
